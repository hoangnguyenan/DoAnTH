@model IEnumerable<HTFood.Models.ViTien>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaKH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoDu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaKH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoDu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaViTien }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaViTien }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaViTien })
        </td>
    </tr>
}

</table>
